
    // if (typeof costPrice !== 'number' || typeof sellingPrice !== 'number' || costPrice < 0 || sellingPrice < 0) {
    //   throw new Error('Invalid input. Please enter positive numbers for cost and selling price.');
    // }
  
    // const profitLoss = sellingPrice - costPrice;
    // return profitLoss > 0 ? `Profit: ${profitLoss}` : profitLoss === 0 ? 'No profit, no loss (break-even)' : `Loss: ${Math.abs(profitLoss)}`;
  
  